{
  "name": "mijntuinproducten-nl",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/remove-spam-blog.mjs && astro build",
    "dev": "astro dev",
    "prepare:blog": "node scripts/remove-spam-blog.mjs",
    "start": "astro dev",
    "preview": "astro preview",
    "astro": "astro",
    "scrape:pages": "python3 scripts/scrape-zb-pages.py --kinds beste,plant",
    "scrape:bedrijven": "python3 scripts/scrape-zb-pages.py --kinds bedrijf --workers 10",
    "download:images": "node scripts/download-images.mjs",
    "verify:links": "python3 scripts/verify-links.py",
    "check:windows-paths": "node scripts/check-windows-paths.mjs",
    "deploy": "npm run build && node scripts/guard-deploy.mjs && npx wrangler deploy"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.14",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.9.3"
  },
  "devDependencies": {
    "xml2js": "^0.6.2"
  }
}
